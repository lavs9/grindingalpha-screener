{
  "mcpServers": {
    "n8n-mcp": {
      "command": "npx",
      "args": ["n8n-mcp"],
      "env": {
        "MCP_MODE": "stdio",
        "LOG_LEVEL": "error",
        "DISABLE_CONSOLE_OUTPUT": "true",
        "N8N_API_URL": "http://localhost:5678",
        "N8N_API_KEY": "<REPLACE-WITH-VALUE-FROM-.env-FILE>"
      }
    }
  }
}

NOTE: Before using this configuration:
1. Copy the N8N_API_KEY value from your .env file
2. Replace <REPLACE-WITH-VALUE-FROM-.env-FILE> with the actual key
3. DO NOT commit the modified file with real credentials
